# java_import_arrays
It will take .npy files and import them into java for computing. Then it will run a forward pass given an input array (Assuming given arrays are weights and biases for a neural network)

(Will be done through javas Nd4j module)
